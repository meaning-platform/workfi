- Make UI work on localhost
- Make UI work regardless of network
- How do we whitelist stablecoins ?
- Fix todos in contract
- Write tests for the contract
- Test that it all works with the UI
- Deploy contract on testnet